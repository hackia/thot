dema "ecran" ;  La tablette ecran.maat contient le code de la fonction nettoyer_ecran.
jena nettoyer_ecran     ; On invoque le rituel (CALL) qui se trouve dans la tablette ecran.maat
loop:
    per "Welcome to the OS!"
    neheh loop     ; Saut inconditionnel (boucle infinie)